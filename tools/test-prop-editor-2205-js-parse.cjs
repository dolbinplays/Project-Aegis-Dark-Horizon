const fs=require('fs'),path=require('path'),assert=require('assert');const root=path.resolve(__dirname,'..');const files=fs.readdirSync(root).filter(n=>/^AEGIS_Prop_(?:Editor|Runtime_Test_Gallery)_v0\.26\.09\.17\.2205_/.test(n));let count=0;for(const name of files){const html=fs.readFileSync(path.join(root,name),'utf8');const re=/<script(?![^>]*\bsrc=)[^>]*>([\s\S]*?)<\/script>/gi;let m;while((m=re.exec(html))){new Function(m[1]);count++;}}assert.ok(count>=2);console.log(`ok - parsed ${count} inline script blocks across ${files.length} 2205 HTML artifacts`);