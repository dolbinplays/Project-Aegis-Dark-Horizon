PROJECT AEGIS 2051 — DIRECT COPY

1. Extract this ZIP over the Project Aegis repository root.
2. Preserve the included paths.
3. Commit/push the changed files.
4. For installed/PWA play, allow the updated service worker to install/activate; relaunch once if the browser is still showing the old cached build.
5. Open Project Aegis. Tools / Editors will appear on supported System/Menu/Pause surfaces.

No installer or batch file is included.
Save format remains 4.

NOTE: A game launched directly with file:// cannot run a service worker, so the in-game launcher extension cannot be injected there. Open AEGIS_Prop_Editor_CURRENT.html directly for local-file authoring.
